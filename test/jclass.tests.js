"use strict";

var expect = require("chai").expect;

describe("jclass master tests", function() {
    it("should equals 1 == 1", function () {
        var one = 1;

        expect(one).to.be.equal(1);
    });
});